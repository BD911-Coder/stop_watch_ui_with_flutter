import 'package:flutter/material.dart';

class ClockMarker extends StatelessWidget {
  const ClockMarker({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
